% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sanitize_gamlss.R
\name{audit_gamlss}
\alias{audit_gamlss}
\title{Audit a sanitized gamlss model for surviving per-observation data}
\usage{
audit_gamlss(x, max_len = 50, max_depth = 15, quiet = FALSE)
}
\arguments{
\item{x}{a \code{gamlss} model object to audit, normally the output of
\code{\link[=sanitize_gamlss]{sanitize_gamlss()}}. Other objects are refused: the length-\code{n} flagging reads
the fitting sample size from \code{x$N}, which only a gamlss fit carries.}

\item{max_len}{check atomic vectors longer than this. Defaults to 50.}

\item{max_depth}{maximum recursion depth. Defaults to 15. Mostly useful when
run on a regular (unsanitized) gamlss model}

\item{quiet}{logical, suppress printed output and return the findings
invisibly. Defaults to \code{FALSE}.}
}
\value{
a character vector of findings, invisibly. Empty when nothing was
flagged.
}
\description{
Recursively walks a gamlssmodel object and checks for atomic vectors of
length \code{n}, which could be a row-level vector that survived and
needs investigating. The exception is \code{pb()}, which are reconstructed by
\code{\link[=sanitize_gamlss]{sanitize_gamlss()}} to length \code{grid_n}, which could coincidentally be equal to
\code{n}: here the audit confirms that the \code{x} values are evenly spaced (i.e. a
reconstructed grid).
}
\details{
The audit only sees vectors longer than \code{max_len} (Default 50). Short but still
disclosive components -- factor levels, \code{random()} level names, the fitting \code{N}
-- are listed in the "What still gets shared" section of
\code{\link[=sanitize_gamlss]{sanitize_gamlss()}} and must be checked manually.
}
\examples{
iris_model <- gamlss(Sepal.Width ~ pb(Sepal.Length) + Species,
                     data = iris, family = NO, trace = FALSE)

# the untouched fit is full of per-observation vectors
audit_gamlss(iris_model)

# the sanitized one should be clean
audit_gamlss(sanitize_gamlss(iris_model))

}
\seealso{
\code{\link[=sanitize_gamlss]{sanitize_gamlss()}}
}
