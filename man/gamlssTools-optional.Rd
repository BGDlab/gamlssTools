% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamlss_helper_funs.R
\name{gamlssTools-optional}
\alias{gamlssTools-optional}
\title{Optional dependencies}
\description{
gamlssTools works with only its required dependencies installed. Two packages
are Suggests rather than Imports, because each is needed by a subset of
functions rather than by the package as a whole.
}
\details{
\href{https://github.com/gamlss-dev/gamlss2}{gamlss2} is required to:
\itemize{
\item fit or work with \code{gamlss2} model objects at all: every \verb{*.gamlss2} method
here relies on gamlss2's own \code{predict()};
\item call \code{\link[=cohens_f2_local]{cohens_f2_local()}}, which uses \code{gamlss2::Rsq()} for \emph{both} gamlss and
gamlss2 fits;
\item call \code{\link[=bootstrap_gamlss]{bootstrap_gamlss()}} on a \code{gamlss2} model, which refits with \code{gamlss2()}.
}

\href{https://github.com/andy1764/gamlss2charts}{gamlss2charts} is required only to
pass \code{batch_term} to \code{\link[=score_centiles]{score_centiles()}}, which estimates and removes the
offsets of unseen batch levels via \code{gamlss2charts::predict_score()}.

Neither package is on CRAN, so both are listed under \verb{Remotes:} and install with:

\if{html}{\out{<div class="sourceCode">}}\preformatted{remotes::install_github("gamlss-dev/gamlss2")
remotes::install_github("andy1764/gamlss2charts")
}\if{html}{\out{</div>}}

Functions that need a suggested package check for it first and fail with an
install hint, so a missing optional dependency never surfaces as a cryptic
error from deep inside a call stack.
}
\keyword{internal}
