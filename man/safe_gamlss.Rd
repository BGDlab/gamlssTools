% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_funs.R
\name{safe_gamlss}
\alias{safe_gamlss}
\title{safe gamlss}
\usage{
safe_gamlss(...)
}
\value{
gamlss model object
}
\description{
gamlss() with more error handling
}
\details{
Fits model using \code{\link[gamlss:gamlss]{gamlss::gamlss()}} and throws an error if model fails to converge or is null

A model that doesn't converge is still returned by \code{\link[gamlss:gamlss]{gamlss::gamlss()}}, so the
error raised for it carries that fit in its \code{model} element -- \code{\link[=gamlss_try]{gamlss_try()}}
uses it to warm-start the retry.

A fit whose residuals are \emph{all} non-finite is rejected too, even when
\code{\link[gamlss:gamlss]{gamlss::gamlss()}} reports it as converged -- the deviance stops moving once the
parameters have overflowed, and nothing downstream can be computed from the
result. A few non-finite residuals among usable ones are left alone.

\code{method} is passed along unevaluated, because \code{RS()}, \code{CG()} and \code{mixed()}
are internal to the gamlss package. Here you can pass \code{method = CG()} or
the string form \code{"CG()"}. Every other argument is evaluated and passed by value,
keeping the model usable later (e.g.  \code{predictAll()} or \code{\link[=bootstrap_gamlss]{bootstrap_gamlss()}}).
The downside is it returns an ugly call argument in \code{\link[gamlss:summary]{gamlss::summary()}}.

Currently only fits gamlss models (not gamlss2).
}
\examples{
iris_model <- safe_gamlss(formula = Sepal.Width ~ Sepal.Length + Petal.Width + Species, 
    sigma.formula = ~ Sepal.Length, data=iris, family=NO)

#the slower CG() fitting method can be requested as a call or as a string:
iris_cg <- safe_gamlss(formula = Sepal.Width ~ Sepal.Length, data = iris, family = NO, method = CG())

}
